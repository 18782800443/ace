on_the_d_c:
  web:
    image: on_the_d_c_(1920, 1080).png
    find_type: xpath
    element: //span[@title="在下边"]
  android:



on_the_d_o:
  web:
#    image: on_the_d_o_(1920, 1080).png
    find_type: xpath
    element: //span[@title="在下边"]

get_data:
  web:
    find_type: xpath
    element: //span[contains(text(), "获取数据")]/parent::button

check_page_d1:
  web:
    find_type: xpath
    element: //body

input_class:
  web:
    find_type: xpath
    element: //div[@class="sa-drawer"]//div[@class="x-form-controls-wrapper sa-row"]/div[1]//input[@type="text"]

input_name:
  web:
    find_type: xpath
    element: //div[@class="sa-drawer"]//div[@class="x-form-controls-wrapper sa-row"]/div[2]//input[@type="text"]

check_page_d2:
  web:
    find_type: xpath
    element: //body

cancel_d:
  web:
    find_type: xpath
    element: //span[contains(text(), "取消")]/parent::button

check_page_d3:
  web:
    find_type: xpath
    element: //body

confirm_d:
  web:
    find_type: xpath
    element: //span[contains(text(), "确定")]/parent::button

check_page_d4:
  web:
    find_type: xpath
    element: //body

check_page_d5:
  web:
    find_type: xpath
    element: //body

check_page_d6:
  web:
    find_type: xpath
    element: //body

check_page_d7:
  web:
    find_type: xpath
    element: //body

#on_the_r:
#  web:
#    image: on_the_r_(1920, 1080).png
#    resolution: (1920, 1081)
on_the_r:
  web:
    find_type: xpath
    element: //span[@title="在右边"]

check_page_r:
  web:
    find_type: xpath
    element: //body

confirm_r:
  web:
    find_type: xpath
    element: //span[contains(text(), "确定")]/parent::button

on_the_u:
  web:
    find_type: xpath
    element: //span[@title="在上边"]

check_page_u:
  web:
    find_type: xpath
    element: //body

confirm_u:
  web:
    find_type: xpath
    element: //span[contains(text(), "确定")]/parent::button

on_the_l:
  web:
    find_type: xpath
    element: //span[@title="在左边"]

check_page_l:
  web:
    find_type: xpath
    element: //body

confirm_l:
  web:
    find_type: xpath
    element: //span[contains(text(), "确定")]/parent::button